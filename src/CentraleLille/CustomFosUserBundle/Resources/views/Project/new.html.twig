{% extends 'layout.html.twig' %}

{% block body %}
    <br><br><br>
    {{ form_start(form, { 'action': path('project_new')}) }}
    {{ form_widget(form) }}
    {{ form_end(form) }}
{% endblock body %}
